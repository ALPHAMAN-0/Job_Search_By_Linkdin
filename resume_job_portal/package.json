{
  "name": "resume_job_portal",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "worker": "npm run dev --prefix worker"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
